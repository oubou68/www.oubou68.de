{
  "name": "www.oubou68.de",
  "version": "1.0.0",
  "description": "Full-stack web project for www.oubou68.de built with React and NestJS",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan.bold,magenta.bold\" \"npm run start:dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "test": "npm run test --prefix server",
    "start:server": "npm run start:prod --prefix server",
    "preview:client": "npm run preview --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "author": "oubou68",
  "license": "MIT"
}
